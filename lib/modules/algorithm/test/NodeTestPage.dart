import 'package:flutter/cupertino.dart';

class NodeTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Node test page"),
    );
  }
}
